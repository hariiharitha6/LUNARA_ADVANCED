<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ════════ BUTTON STYLES ════════ -->
    <style name="LunaraPrimaryButton">
        <item name="android:background">@drawable/btn_gradient_rounded</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
    </style>

    <!-- ════════ CARD STYLES ════════ -->
    <style name="LunaraCard">
        <item name="android:background">@drawable/bg_card_elevated</item>
        <item name="android:elevation">4dp</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="LunaraElevatedCard">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">6dp</item>
        <item name="cardBackgroundColor">@color/card_bg</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- ════════ TYPOGRAPHY STYLES ════════ -->
    <style name="LunaraHeading">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="LunaraSubtitle">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>

    <!-- ════════ RISK COLORS (Text Styles) ════════ -->
    <style name="LunaraRiskHigh">
        <item name="android:textColor">@color/danger</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="LunaraRiskModerate">
        <item name="android:textColor">@color/warning</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="LunaraRiskLow">
        <item name="android:textColor">@color/success</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
